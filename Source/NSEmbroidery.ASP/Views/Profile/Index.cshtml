
<div class="buttom"
            <div class="buttom-left">
                <div>
                @if (Request.IsAuthenticated)
                {
                    <nav id="menu">
                      @{
                          @Ajax.ActionLink("My Gallery", "Gallery", null, new AjaxOptions() { HttpMethod = "POST", InsertionMode = InsertionMode.Replace, UpdateTargetId = "content" }, new { @class = "item" })
                          @Ajax.ActionLink("Users", "Users", null, new AjaxOptions() { HttpMethod = "POST", InsertionMode = InsertionMode.Replace, UpdateTargetId = "content" }, new { @class = "item" })
                          @Ajax.ActionLink("Add Embroidery", "AddEmbroidery", null, new AjaxOptions() { HttpMethod = "GET", InsertionMode = InsertionMode.Replace, UpdateTargetId = "content" }, new { @class = "item" })
                      }
                    </nav>
                }
                </div>
                
                <div id="content">
                </div>

            </div>


 </div>
