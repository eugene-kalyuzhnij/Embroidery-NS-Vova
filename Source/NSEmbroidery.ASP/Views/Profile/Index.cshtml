
<div class="buttom"
            <div class="buttom-left">
                <div>
                @if (Request.IsAuthenticated)
                {
                    <nav id="menu">
                      @{
                          @Ajax.ActionLink("My Gallery", "Index", "Gallery", null, new AjaxOptions() { HttpMethod = "POST", InsertionMode = InsertionMode.Replace, UpdateTargetId = "content" }, new { @class = "item" })
                          @Ajax.ActionLink("Users", "Index", "Users", null, new AjaxOptions() { HttpMethod = "POST", InsertionMode = InsertionMode.Replace, UpdateTargetId = "content" }, new { @class = "item" })
                          @Ajax.ActionLink("Add Embroidery", "Index", "AddEmbroidery", null, new AjaxOptions() { HttpMethod = "GET", InsertionMode = InsertionMode.Replace, UpdateTargetId = "content" }, new { @class = "item" })
                      }
                    </nav>
                }
                </div>
                
                <div id="content">
                </div>

            </div>


 </div>
